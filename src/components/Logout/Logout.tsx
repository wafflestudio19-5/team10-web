import styles from "./Logout.module.scss";

const Logout = () => {
  return (
    <div className={styles.box}>
      <p>You've signed out. Now go mobile!</p>
    </div>
  );
};

export default Logout;
